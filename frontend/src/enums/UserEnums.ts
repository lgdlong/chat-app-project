export enum UserStatus {
  ACTIVE = "ACTIVE",
  BANNED = "BANNED",
}

export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum UserStatusFrontend {
  ONLINE = "online",
  BUSY = "busy",
  OFFLINE = "offline",
}
